/*context('send chat message', () => {
  let msgText = 'This message was created at: ' + new Date().getTime();
  it('send to mod1 group', () => {
    cy.loginTestUser();
    cy.sendChatMessage('mod1', msgText);
  })

  it('send to Alfons 2', () => {
    cy.loginTestUser();
    cy.sendChatMessage('Alfons 2', msgText);
  })

  it('load oldest message', () => {
    cy.loginTestUser();
    cy.chatScrollUntil('mod1', 'Message999');
  })
});
*/
